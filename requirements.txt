# Capped below 5.0: the dev/prod MariaDB is 10.4.32, and Django 5.0+ requires MariaDB 10.5+.
Django>=4.2,<5.0
mysqlclient>=2.2.8
python-dotenv>=1.2.3
openpyxl>=3.1.5
xhtml2pdf>=0.2.17
matplotlib>=3.11.1
# Production server (see deploy/PRODUCTION_SETUP.md): waitress is the WSGI
# server, whitenoise serves static files directly so no IIS/nginx is needed.
waitress>=3.0.2
whitenoise>=6.11.0
